version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: azerbaijan_tourism_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: azerbaijan_tourism
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: azerbaijan_tourism_redis
    restart: unless-stopped
    ports:
      - '6379:6379'

volumes:
  postgres_data:
